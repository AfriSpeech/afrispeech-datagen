# GPU recommended but not required (OmniVoice auto-detects CUDA and falls back to CPU).
# System package: ffmpeg.
omnivoice @ git+https://github.com/groxaxo/omnivoice-streaming.git
datasets>=2.19
huggingface_hub>=0.23
soundfile>=0.12
librosa>=0.10
numpy
scipy
tqdm
torch
torchaudio
transformers
tokenizers
accelerate
